import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Webpack","description":"","frontmatter":{},"headers":[],"relativePath":"interview/webpack.md","filePath":"interview/webpack.md","lastUpdated":1774408671000}');
const _sfc_main = { name: "interview/webpack.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="webpack" tabindex="-1">Webpack <a class="header-anchor" href="#webpack" aria-label="Permalink to “Webpack”">​</a></h1><h2 id="对-webpack-的理解-解决了什么问题" tabindex="-1">对 webpack 的理解？解决了什么问题？ <a class="header-anchor" href="#对-webpack-的理解-解决了什么问题" aria-label="Permalink to “对 webpack 的理解？解决了什么问题？”">​</a></h2><p>Webpack 是一个<strong>静态模块打包工具</strong>。</p><p>它会从入口文件出发，分析项目中的依赖关系，构建依赖图，然后把 JavaScript、CSS、图片、TypeScript、Vue 组件等资源都当作模块处理，最终打包成浏览器可运行的静态资源。</p><p>主要解决了：</p><ul><li>统一管理模块和依赖关系</li><li>让浏览器不能直接识别的资源也能参与构建，比如 TS、Less、Vue SFC</li><li>提供开发服务器和 HMR，提升开发体验</li><li>支持压缩、Tree Shaking、代码分割等优化能力</li></ul><p>总结：</p><p><strong>Webpack 本质上就是一个模块打包器，核心思想是“万物皆可模块”。</strong></p><h2 id="构建流程" tabindex="-1">构建流程 <a class="header-anchor" href="#构建流程" aria-label="Permalink to “构建流程”">​</a></h2><p>Webpack 的构建流程可以概括为：</p><p><strong>读取配置 -&gt; 找到入口 -&gt; 解析依赖 -&gt; 调用 Loader 转换模块 -&gt; 组装 Chunk -&gt; 输出 Bundle</strong></p><ol><li>读取配置文件和命令行参数，得到最终配置</li><li>创建 <code>Compiler</code> 对象，开始构建流程</li><li>根据 <code>entry</code> 找到入口文件</li><li>从入口出发，调用 Loader 处理不同类型的文件</li><li>解析源码，收集 <code>import</code> / <code>require</code> 依赖</li><li>递归处理依赖模块，形成完整依赖图</li><li>根据依赖关系组装成 <code>Chunk</code>，再输出成最终的 <code>Bundle</code></li></ol><p>总结：</p><p><strong>Webpack 就是从入口出发，递归分析依赖，处理模块，最后打包输出。</strong></p><h2 id="loader" tabindex="-1">Loader <a class="header-anchor" href="#loader" aria-label="Permalink to “Loader”">​</a></h2><p>Loader 本质上是一个<strong>转换器</strong>，用来把某种资源转换成 Webpack 能处理的模块。</p><p>因为 Webpack 默认只能直接处理 JavaScript 和 JSON，所以像 CSS、Less、TypeScript、Vue 文件这些资源，都需要借助 Loader。</p><p>常见 Loader 有：</p><ul><li><code>babel-loader</code>：把 ES6+ 转成兼容性更好的 JavaScript</li><li><code>ts-loader</code>：把 TypeScript 转成 JavaScript</li><li><code>css-loader</code>：解析 CSS 中的 <code>@import</code> 和 <code>url()</code></li><li><code>style-loader</code>：把 CSS 通过 <code>&lt;style&gt;</code> 标签插入页面</li><li><code>less-loader</code> / <code>sass-loader</code>：把 Less / Sass 转成 CSS</li><li><code>postcss-loader</code>：做 CSS 兼容性处理，比如自动补前缀</li><li><code>vue-loader</code>：处理 <code>.vue</code> 单文件组件</li></ul><p>解决的问题是：</p><ul><li>让非 JS 资源也能被 Webpack 识别和打包</li><li>让高级语法和工程化语法最终都能转成浏览器可运行的代码</li></ul><p><strong>多个 Loader 的执行顺序是从右到左、从下到上。</strong></p><h2 id="plugin" tabindex="-1">Plugin <a class="header-anchor" href="#plugin" aria-label="Permalink to “Plugin”">​</a></h2><p>Plugin 是对 Webpack <strong>整个构建生命周期</strong>的扩展。</p><p>如果说 Loader 负责“翻译文件”，那 Plugin 负责“扩展流程”。</p><p>常见 Plugin 有：</p><ul><li><code>html-webpack-plugin</code>：自动生成 HTML，并注入打包后的资源</li><li><code>clean-webpack-plugin</code>：打包前清理输出目录</li><li><code>mini-css-extract-plugin</code>：把 CSS 提取成独立文件</li><li><code>DefinePlugin</code>：注入全局常量或环境变量</li><li><code>terser-webpack-plugin</code>：压缩 JavaScript</li><li><code>css-minimizer-webpack-plugin</code>：压缩 CSS</li><li><code>compression-webpack-plugin</code>：生成 gzip 等压缩资源</li></ul><p>解决的问题是：</p><ul><li>自动化处理构建过程中的通用任务</li><li>提升打包结果质量，比如压缩、提取、注入、清理</li><li>让 Webpack 具备更强的扩展能力和工程化能力</li></ul><p>总结：</p><p><strong>Loader 解决资源转换问题，Plugin 解决构建流程扩展问题。</strong></p><h2 id="loader-和-plugin-的区别-编写它们的思路是什么" tabindex="-1">Loader 和 Plugin 的区别？编写它们的思路是什么？ <a class="header-anchor" href="#loader-和-plugin-的区别-编写它们的思路是什么" aria-label="Permalink to “Loader 和 Plugin 的区别？编写它们的思路是什么？”">​</a></h2><p>它们的核心区别是：</p><ul><li><code>Loader</code> 是<strong>文件级别</strong>的处理器，主要负责把某类文件转换成 Webpack 能识别的模块</li><li><code>Plugin</code> 是<strong>流程级别</strong>的扩展器，主要负责介入整个构建生命周期，完成更复杂的任务</li></ul><h3 id="区别" tabindex="-1">区别 <a class="header-anchor" href="#区别" aria-label="Permalink to “区别”">​</a></h3><table tabindex="0"><thead><tr><th>维度</th><th>Loader（转换器）</th><th>Plugin（插件 / 扩展）</th></tr></thead><tbody><tr><td>本质</td><td>一个导出为函数的 JavaScript 模块</td><td>一个带有 <code>apply</code> 方法的 Class 类或对象</td></tr><tr><td>职能</td><td>单一，专注于将非 JS / JSON 资源翻译成 Webpack 能识别的模块</td><td>更广，基于事件机制介入整个打包过程，执行各种自动化构建任务</td></tr><tr><td>运行时机</td><td>运行在模块解析和加载阶段，也就是打包具体文件时</td><td>贯穿 Webpack 的整个编译生命周期</td></tr><tr><td>操作对象</td><td>具体的文件内容，本质上是把一种内容转换成另一种内容</td><td>整个构建体系，通过 Webpack 提供的 Hook 改变构建过程或输出结果</td></tr><tr><td>配置位置</td><td><code>module.rules</code></td><td><code>plugins</code></td></tr><tr><td>解决问题</td><td>解决资源转换问题</td><td>解决构建流程扩展问题</td></tr></tbody></table><h3 id="编写-loader-的思路" tabindex="-1">编写 Loader 的思路 <a class="header-anchor" href="#编写-loader-的思路" aria-label="Permalink to “编写 Loader 的思路”">​</a></h3><p>编写 Loader 的核心思路就是：<strong>接收源文件内容，处理后再把结果返回出去。</strong></p><ol><li>Loader 本质是一个函数，输入是源代码内容</li><li>对内容进行转换，比如把 Less 转成 CSS，或者做字符串替换</li><li>返回转换后的结果，交给下一个 Loader 或 Webpack 继续处理</li></ol><h3 id="编写-plugin-的思路" tabindex="-1">编写 Plugin 的思路 <a class="header-anchor" href="#编写-plugin-的思路" aria-label="Permalink to “编写 Plugin 的思路”">​</a></h3><p>编写 Plugin 的核心思路就是：<strong>找到合适的生命周期钩子，在对应时机插入自己的逻辑。</strong></p><ol><li>Plugin 一般是一个类</li><li>需要实现 <code>apply</code> 方法</li><li>在 <code>apply</code> 方法里拿到 <code>compiler</code></li><li>再通过 <code>compiler</code> 或 <code>compilation</code> 的钩子函数注册逻辑</li><li>在合适的时机执行自己的功能，比如修改资源、输出日志、生成文件等</li></ol><p>常见理解方式是：</p><ul><li><code>compiler</code> 偏全局，贯穿整个构建过程</li><li><code>compilation</code> 偏单次构建结果，关注本次打包产物</li></ul><h2 id="热更新-hmr-是如何做到的-原理是什么" tabindex="-1">热更新（HMR）是如何做到的？原理是什么？ <a class="header-anchor" href="#热更新-hmr-是如何做到的-原理是什么" aria-label="Permalink to “热更新（HMR）是如何做到的？原理是什么？”">​</a></h2><h3 id="核心原理" tabindex="-1">核心原理 <a class="header-anchor" href="#核心原理" aria-label="Permalink to “核心原理”">​</a></h3><p>HMR（Hot Module Replacement）即<strong>模块热替换</strong>。</p><p>核心作用是：<strong>在应用运行过程中，替换、添加或删除模块，而不需要刷新整个页面。</strong></p><p>最大的优势是可以保留当前页面状态，比如表单输入、页面滚动位置、组件局部状态等，所以开发体验会更好。</p><p>Webpack 的 HMR 可以概括为：</p><p><strong>服务端监听文件变化并重新编译，然后通过 WebSocket 通知客户端，客户端再去拉取补丁并替换模块。</strong></p><p><strong>一个长连接通知，两次补丁拉取，最后局部替换模块。</strong></p><h3 id="具体流程" tabindex="-1">具体流程 <a class="header-anchor" href="#具体流程" aria-label="Permalink to “具体流程”">​</a></h3><ol><li>启动开发服务</li></ol><p>启动 <code>webpack-dev-server</code> 后，实际上会提供两类能力：</p><ul><li>HTTP 服务：负责返回打包后的静态资源</li><li>WebSocket 服务：负责和浏览器建立长连接，用来通知更新</li></ul><ol start="2"><li>监听文件变化</li></ol><p>当我们修改源码并保存后，Webpack 会监听到文件变化，然后重新编译改动的模块。</p><ol start="3"><li>生成更新补丁</li></ol><p>编译完成后，会生成本次热更新需要的补丁信息，主要包括：</p><ul><li><code>manifest</code>：描述这次哪些模块或 chunk 发生了变化</li><li><code>update.js</code>：真正需要替换的增量模块代码</li></ul><ol start="4"><li>服务端通知客户端</li></ol><p>Webpack Dev Server 会通过 WebSocket 把最新的 <code>hash</code> 发送给浏览器。</p><ol start="5"><li>客户端拉取补丁</li></ol><p>浏览器端的 HMR Runtime 收到通知后，会开始更新流程：</p><ul><li>先根据最新 <code>hash</code> 请求 <code>manifest</code></li><li>再根据 <code>manifest</code> 拉取对应的 <code>update.js</code></li></ul><ol start="6"><li>替换旧模块</li></ol><p>HMR Runtime 拿到新的模块代码后，会把旧模块卸载，再把新模块替换进去。</p><p>如果这个模块支持热替换，就只更新当前模块及相关依赖，而不会刷新整个页面。</p><h3 id="为什么-hmr-可以不刷新页面" tabindex="-1">为什么 HMR 可以不刷新页面 <a class="header-anchor" href="#为什么-hmr-可以不刷新页面" aria-label="Permalink to “为什么 HMR 可以不刷新页面”">​</a></h3><p>因为它更新的不是整个网页，而是<strong>发生变化的模块</strong>。</p><p>也就是说，浏览器不会重新加载整个页面，而是只下载并执行改动后的那部分模块代码，所以页面状态通常可以保留下来。</p><h3 id="总结" tabindex="-1">总结 <a class="header-anchor" href="#总结" aria-label="Permalink to “总结”">​</a></h3><p>HMR 就是模块热替换。它的原理是 <code>webpack-dev-server</code> 会通过 WebSocket 和浏览器建立长连接，代码改动后服务端重新编译，并把新的 <code>hash</code> 通知给客户端。客户端再去请求 <code>manifest</code> 和 <code>update.js</code> 这些补丁文件，最后把变更的模块替换掉，而不是刷新整个页面，所以可以保留页面状态。</p><h2 id="webpack-proxy-工作原理-为什么能解决跨域" tabindex="-1">Webpack Proxy 工作原理？为什么能解决跨域？ <a class="header-anchor" href="#webpack-proxy-工作原理-为什么能解决跨域" aria-label="Permalink to “Webpack Proxy 工作原理？为什么能解决跨域？”">​</a></h2><h3 id="核心原理-1" tabindex="-1">核心原理 <a class="header-anchor" href="#核心原理-1" aria-label="Permalink to “核心原理”">​</a></h3><p>Webpack Proxy 的工作原理是：在本地开发环境中，利用 <code>webpack-dev-server</code> 内部的代理能力（底层常用 <code>http-proxy-middleware</code>），启动一个 <strong>Node.js 代理服务器</strong>。</p><p>它作为浏览器和真实后端之间的“中间人”转发请求，利用了一个关键点：</p><p><strong>同源策略只限制浏览器，服务器和服务器之间通信不受同源策略限制。</strong></p><h3 id="跨域背景" tabindex="-1">跨域背景 <a class="header-anchor" href="#跨域背景" aria-label="Permalink to “跨域背景”">​</a></h3><p>跨域（CORS 报错）来自浏览器的<strong>同源策略（Same-Origin Policy）</strong>安全限制。</p><p>当协议、域名、端口任意一个不同，浏览器会对 Ajax / Fetch 的响应做拦截，从而出现跨域问题。</p><p>要点：</p><ul><li>跨域限制发生在浏览器端</li><li>服务端之间请求不受同源策略限制</li></ul><h3 id="proxy-是如何解决跨域的" tabindex="-1">Proxy 是如何解决跨域的？ <a class="header-anchor" href="#proxy-是如何解决跨域的" aria-label="Permalink to “Proxy 是如何解决跨域的？”">​</a></h3><p>配置了 Proxy 之后，请求链路会变成：</p><ol><li>浏览器 -&gt; 本地开发服务器</li></ol><p>前端把请求发给本地 dev server（例如 <code>http://localhost:8080/api</code>），这是同源请求，不会跨域。</p><ol start="2"><li>本地代理 -&gt; 真实后端</li></ol><p>dev server 根据 <code>proxy</code> 规则（例如以 <code>/api</code> 开头）拦截请求，并转发给真实接口服务器（例如 <code>http://api.example.com</code>）。</p><p>这一步是“服务器 -&gt; 服务器”的通信，不受同源策略限制。</p><ol start="3"><li>真实后端 -&gt; 本地代理</li></ol><p>真实后端处理请求后，把响应返回给本地代理服务器。</p><ol start="4"><li>本地代理 -&gt; 浏览器</li></ol><p>本地代理再把响应原封不动回传给浏览器，因此浏览器不会触发跨域拦截。</p><h3 id="总结-1" tabindex="-1">总结 <a class="header-anchor" href="#总结-1" aria-label="Permalink to “总结”">​</a></h3><p>Webpack Proxy 本质是在本地起一个 Node 代理（底层常用 <code>http-proxy-middleware</code>）。浏览器只请求本地 dev server，所以同源；dev server 再把请求转发到真实后端。跨域是浏览器同源策略导致的，而服务器和服务器之间没有跨域限制，所以通过代理转发就能绕过浏览器的跨域拦截。</p><h2 id="webpack-优化前端性能" tabindex="-1">Webpack 优化前端性能？ <a class="header-anchor" href="#webpack-优化前端性能" aria-label="Permalink to “Webpack 优化前端性能？”">​</a></h2><h3 id="核心思路" tabindex="-1">核心思路 <a class="header-anchor" href="#核心思路" aria-label="Permalink to “核心思路”">​</a></h3><p>借助 Webpack 优化前端性能，核心围绕两个方向展开：</p><ul><li><strong>缩减打包体积，让包更小</strong></li><li><strong>优化加载策略，让资源加载更聪明</strong></li></ul><h3 id="_1-代码体积压缩" tabindex="-1">1. 代码体积压缩 <a class="header-anchor" href="#_1-代码体积压缩" aria-label="Permalink to “1. 代码体积压缩”">​</a></h3><p>目标是让最终产物更小，减少传输体积。</p><ul><li>JS 压缩：使用 <code>TerserPlugin</code></li><li>CSS 压缩：使用 <code>CssMinimizerPlugin</code></li><li>HTML 压缩：配合 <code>HtmlWebpackPlugin</code> 压缩空格和注释</li><li>Gzip 压缩：使用 <code>CompressionPlugin</code> 预生成 <code>.gz</code> 文件</li><li>图片压缩：使用 <code>image-webpack-loader</code></li><li>小图内联：使用 Webpack 5 的 <code>asset/inline</code> 转成 Base64，减少请求</li></ul><h3 id="_2-tree-shaking-剔除死代码" tabindex="-1">2. Tree Shaking 剔除死代码 <a class="header-anchor" href="#_2-tree-shaking-剔除死代码" aria-label="Permalink to “2. Tree Shaking 剔除死代码”">​</a></h3><p>目标是删除项目中没有实际使用到的代码。</p><ul><li>JS Tree Shaking：依赖 ES Modules 的静态分析</li><li>可以结合 <code>sideEffects: false</code>，帮助 Webpack 删除无副作用代码</li><li>CSS Tree Shaking：可以配合 <code>PurgeCSSPlugin</code> 删除未使用的样式</li></ul><h3 id="_3-代码分割与提取" tabindex="-1">3. 代码分割与提取 <a class="header-anchor" href="#_3-代码分割与提取" aria-label="Permalink to “3. 代码分割与提取”">​</a></h3><p>目标是避免把所有代码一次性加载进来。</p><ul><li>公共依赖提取：通过 <code>optimization.splitChunks</code> 把公共模块和第三方依赖单独拆包</li><li>路由懒加载：使用 <code>import()</code> 动态导入组件，实现按需加载</li></ul><p>这样做的好处是：</p><ul><li>首屏更快</li><li>缓存利用率更高</li><li>资源可以并行加载</li></ul><h3 id="_4-资源内联与加载优化" tabindex="-1">4. 资源内联与加载优化 <a class="header-anchor" href="#_4-资源内联与加载优化" aria-label="Permalink to “4. 资源内联与加载优化”">​</a></h3><p>目标是减少请求次数，进一步优化加载体验。</p><ul><li>小资源内联，减少请求</li><li>Runtime 代码内联，减少一次额外请求</li><li>文件名加 <code>hash</code>，更好利用浏览器长缓存</li></ul><h3 id="总结-2" tabindex="-1">总结 <a class="header-anchor" href="#总结-2" aria-label="Permalink to “总结”">​</a></h3><p>Webpack 做前端性能优化，核心就是两件事：一是减小包体积，二是优化加载策略。具体可以从压缩资源、Tree Shaking、代码分割与懒加载、资源内联和缓存这几个方向展开回答。</p><h2 id="webpack-和-vite-的区别" tabindex="-1">Webpack 和 Vite 的区别 <a class="header-anchor" href="#webpack-和-vite-的区别" aria-label="Permalink to “Webpack 和 Vite 的区别”">​</a></h2><h3 id="_1-底层机制不同" tabindex="-1">1. 底层机制不同 <a class="header-anchor" href="#_1-底层机制不同" aria-label="Permalink to “1. 底层机制不同”">​</a></h3><p>Webpack 的开发模式是先从入口出发，扫描依赖图，经过 Loader 和 Plugin 处理后，把模块打包成一个或多个 Bundle，再交给浏览器运行。</p><p>Vite 的开发模式不是先整体打包，而是直接启动开发服务器，利用浏览器原生支持 <code>ESM</code> 的能力。浏览器在解析 <code>import</code> 时，才会向服务端按需请求对应模块，Vite 再实时编译并返回。</p><p>所以本质区别是：</p><ul><li>Webpack：先打包，再运行</li><li>Vite：先启动服务，按需编译，再运行</li></ul><h3 id="_2-为什么-vite-冷启动快" tabindex="-1">2. 为什么 Vite 冷启动快 <a class="header-anchor" href="#_2-为什么-vite-冷启动快" aria-label="Permalink to “2. 为什么 Vite 冷启动快”">​</a></h3><p>Webpack 冷启动慢，是因为它启动前需要先处理整个项目依赖图，项目越大，初始化越慢。</p><p>Vite 冷启动快，是因为它开发阶段不做全量打包，而是直接启动服务，浏览器访问到哪个模块，再编译哪个模块。</p><p>所以 Vite 的启动成本和项目规模的关系没那么强，冷启动通常会非常快。</p><h3 id="_3-为什么-vite-热更新快" tabindex="-1">3. 为什么 Vite 热更新快 <a class="header-anchor" href="#_3-为什么-vite-热更新快" aria-label="Permalink to “3. 为什么 Vite 热更新快”">​</a></h3><p>Webpack 的 HMR，本质上还是要重新编译受影响的模块和部分依赖链，再生成新的 Bundle 补丁。</p><p>Vite 的 HMR 基于原生 ESM。修改一个文件后，Vite 只需要让浏览器重新请求这一个变更模块即可，不需要重新打包整个依赖图。</p><p>所以 Vite 热更新快的原因是：</p><ul><li>更新粒度更细</li><li>不需要重新生成大块 Bundle</li><li>只处理变更模块本身及少量关联模块</li></ul><h3 id="_4-生产环境构建不同" tabindex="-1">4. 生产环境构建不同 <a class="header-anchor" href="#_4-生产环境构建不同" aria-label="Permalink to “4. 生产环境构建不同”">​</a></h3><p>Webpack 在开发和生产环境里，核心都是依赖自己的打包体系。</p><p>Vite 在开发环境追求极致快，依赖原生 ESM；但在生产环境，Vite 并不会直接把大量 ESM 请求交给浏览器，而是底层使用 <strong>Rollup</strong> 进行打包构建。</p><p>也就是说：</p><ul><li>Vite 开发阶段快，靠的是原生 ESM + 按需编译</li><li>Vite 生产阶段打包，底层靠的是 Rollup</li></ul><h3 id="_5-生态和适用场景不同" tabindex="-1">5. 生态和适用场景不同 <a class="header-anchor" href="#_5-生态和适用场景不同" aria-label="Permalink to “5. 生态和适用场景不同”">​</a></h3><p>Webpack 的优势是：</p><ul><li>生态成熟</li><li>对老项目兼容性更好</li><li>适合复杂历史项目和深度定制场景</li></ul><p>Vite 的优势是：</p><ul><li>开发体验更好</li><li>冷启动快</li><li>热更新快</li><li>更适合现代前端项目</li></ul><p>但 Vite 对现代浏览器和 ESM 依赖更强，在处理一些老旧 CommonJS 包时，可能需要额外兼容处理。</p><h3 id="总结-3" tabindex="-1">总结 <a class="header-anchor" href="#总结-3" aria-label="Permalink to “总结”">​</a></h3><p>Webpack 和 Vite 最大的区别在于开发机制。Webpack 是先打包再运行，Vite 是基于浏览器原生 ESM 的按需编译。Vite 冷启动快，是因为它不需要先全量打包；热更新快，是因为它只需要让浏览器重新请求变更模块，而不是重新生成整个 Bundle。生产环境下，Vite 仍然会用 Rollup 做打包。总体来说，Vite 更适合现代项目，Webpack 更适合生态复杂、历史包袱重的项目。</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("interview/webpack.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const webpack = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  webpack as default
};
